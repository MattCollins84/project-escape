{
  "name": "bunker",
  "version": "1.0.0",
  "description": "bunker related magic",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "node-lifx": "^0.8.0",
    "onoff": "^3.0.2"
  }
}
